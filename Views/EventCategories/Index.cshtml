@model IEnumerable<SkyGlobal.Models.EventCategorie>

@{
    ViewData["Title"] = "Index";
}
<!-- breadcrumb -->
<section class="w3l-about-breadcrumb">
    <div class="breadcrumb-bg breadcrumb-bg-about py-5">
        <div class="container py-lg-5 py-md-4">
            <div class="w3breadcrumb-gids text-center pt-5">
                <div class="w3breadcrumb-right">
                    <ul class="breadcrumbs-custom-path">
                        <li><a href="index.html" class="breadcrumb-link">Home</a></li>
                        <li class="active"><span class="fas fa-angle-double-right mx-2"></span> Event Categorie</li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
</section>
<!--//breadcrumb-->

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.EventCategorieName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.EventCategorieName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.EventCategorieId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.EventCategorieId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.EventCategorieId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
